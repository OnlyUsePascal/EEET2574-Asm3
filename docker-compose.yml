services:
  air:
    build: ./producer-air
    container_name: air
    env_file:
      - .env
    ports:
      - "8081:8081" 
    restart: always
    networks:
      - weather-network

networks:
  weather-network:
    driver: bridge